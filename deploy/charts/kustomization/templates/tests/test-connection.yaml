apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kustomization.fullname" . }}-test-connection"
  labels:
    {{- include "kustomization.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kustomization.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
