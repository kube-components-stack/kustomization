apiVersion: v1
kind: Service
metadata:
  name: {{ include "kustomization.fullname" . }}
  labels:
    {{- include "kustomization.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kustomization.selectorLabels" . | nindent 4 }}
